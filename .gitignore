# ── Dependencies ────────────────────────────────
node_modules/
# package-lock.json is committed intentionally (reproducible installs)

# ── Build output (generated by `npm run build`) ──
dist/
*.zip

# ── Env & OS ─────────────────────────────────────
.env
.env.local
.env.*.local
.DS_Store
Thumbs.db
desktop.ini

# ── Vite / Editor ────────────────────────────────
.vite/
coverage/
.idea/
.vscode/
*.log
npm-debug.log*
pnpm-debug.log*

# ── Lively local cache (if any) ──────────────────
# Lively copies wallpapers to %APPDATA%\Lively Wallpaper\ — never commit
